<h1><%= "#{@song.title} - #{@song.artist}" %></h1>
<p><%= @song.description %></p>

<h3>Options</h3>
<ul>
  <li><%= link_to "Edit", edit_song_path %></li>
  <li><%= link_to "Delete", song_path(@song), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
  <li><%= link_to "Go Back", root_path %></li>
</ul>